import {Job, scheduledJobs} from 'node-schedule';

export class scheduleHelper {
    
    private plop(): void {
        scheduledJobs['plop']
    }

    //list jobs 

    //delete job

    //add job
}